export enum RoleEnum {
  MANAGER = "MANAGER",
  ADMIN = "ADMIN",
  CASHIER = "CASHIER",
  EMPLOYEE = "EMPLOYEE",
}

// TODO: Talvez lo borremos luego, no se si es necesario
export enum TokenTypeEnum {
  TEMPORARY = "TEMPORARY",
  FULL = "FULL",
}
